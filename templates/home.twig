{% extends "base.twig" %}

{% block content %}
  <div class="container">
    <h1 class="title">
      {{ message.title }}
    </h1>

    <div>
      {{ message.body | raw }}
    </div>
  </div>
{% endblock %}
